import { Layout } from '../../components';

const FavoritesPage: React.FC = (): React.ReactElement => {
	return (
		<Layout headTitle="Your favorites">
			<h1>Favorites</h1>
		</Layout>
	);
};

export default FavoritesPage;
